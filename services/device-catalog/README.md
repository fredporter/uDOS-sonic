# Device Catalog Service

The device catalog service owns hardware profile and compatibility data.

Current implementation:

- `datasets/`
- `services/runtime_service.py`

Educational role:

- teach how device metadata informs deployment choices without redefining the runtime
