# Manifest Service

The manifest service validates and explains deployment manifests.

Current implementation:

- `services/manifest.py`
- `services/verify.py`
- `services/runtime_service.py`

Educational role:

- teach that the manifest is the review surface between intent and apply
