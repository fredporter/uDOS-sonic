# Build Engine Service

The build engine service is the Sonic-owned execution layer.

Current implementation:

- `scripts/partition-layout.sh`
- `scripts/apply-payloads-v2.sh`
- `scripts/sonic-stick.sh`

Educational role:

- teach that destructive execution is explicit and separate from planning
