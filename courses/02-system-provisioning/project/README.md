# Course 02 Project

Project: generate a validated manifest and describe the execution path.

Suggested flow:

1. run the normal `plan` command with `--dry-run`
2. inspect the output manifest path
3. list the scripts that would be involved in a real apply
4. explain which concerns belong to Sonic and which belong to `uHOME-server`
