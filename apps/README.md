# Sonic Apps

`apps/` contains the user-facing entry surfaces for `uDOS-sonic`.

Current mapping:

- `apps/sonic-cli/` is the active CLI entrypoint
- `apps/sonic-ui/` is the active browser UI

This root now matches the runtime-facing entry surfaces learners and operators
should use.
